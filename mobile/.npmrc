# Expo SDK 57's web deps (react-dom 19.2.7 via expo-router/radix) declare a
# stricter react peer than the pinned react 19.2.3. This is benign; relax npm's
# peer resolution so installs are reproducible.
legacy-peer-deps=true
